# 小球障碍物映射问题诊断与修复

## 问题描述
小球障碍物（Label 104）在点云三视图中无法正确显示，尽管检测框存在。

## 根本原因分析

### 1. 深度数据缺失
- 小球区域可能视差匹配失败，导致深度值无效
- 没有深度就无法生成3D点云

### 2. 点云过滤器过于严格
- **体素降采样**：2.5cm体素可能将小球的稀疏点云大量合并
- **离群点移除**：要求8cm半径内至少6个邻居，小球点云稀疏容易被误判为离群点

### 3. 采样步长问题
- 常规采样步长为4像素，小目标采样点少
- 虽然有密集采样（步长2）针对label>=100，但如果深度无效仍然无法生成点

## 已实施的修复方案

### 修改1: 添加详细诊断日志 (stereo_multi_match.cpp)

**位置**: `stereo_process_pci_depth_rgb_seg_det_fusion` 函数

**功能**:
- 统计每个label在标签图中的像素数
- 统计每个label有有效深度的像素数
- 统计每个label被采样的点云数
- 输出采样率和深度有效率

**输出示例**:
```
========== 障碍物采样诊断 ==========
Label 104:
  - 标签图像素数: 1200
  - 有效深度像素: 450 (37.5%)
  - 采样点云数: 225 (50.0%)
========================================
```

### 修改2: 添加过滤过程诊断 (stereo_multi_match.cpp)

**位置**: `stereo_point_ori_rgb_filter` 函数

**功能**:
- 追踪每个label在各过滤阶段的点数变化
- 识别哪个过滤器导致点云丢失
- 对完全被过滤的label发出警告

**输出示例**:
```
========== 点云过滤诊断 ==========
总点数变化: 5000 -> 2000 (体素) -> 1800 (离群点) -> 1750 (标签)

Label 104 过滤过程:
  原始: 225 点
  体素降采样后: 45 点 (损失 180, 80.0%)
  离群点移除后: 0 点 (损失 45, 100.0%)
  ⚠️  警告: Label 104 的所有点被过滤器移除!
========================================
```

### 修改3: 针对小球的特殊过滤策略 (stereo_multi_match.cpp)

**位置**: `stereo_point_ori_rgb_filter` 函数的离群点移除步骤

**策略**:
1. **分离处理**: 将Label 104的点云与其他点云分开处理
2. **宽松参数**:
   - 搜索半径从8cm增加到15cm
   - 邻居数要求从6个降低到3个
3. **条件跳过**: 如果Label 104点数<10，完全跳过离群点过滤

**代码逻辑**:
```cpp
if (obstacle_cloud->size() >= 10) {
    // 使用宽松参数过滤
    ror.setRadiusSearch(0.15);  // 15cm
    ror.setMinNeighborsInRadius(3);  // 3个邻居
} else {
    // 点数太少，跳过过滤
    *filtered_obstacle = *obstacle_cloud;
}
```

### 修改4: 添加Label 107支持

**位置**:
- `stereo_point_cloud_rgbl.cpp` - initColorMap()
- `stereo_point_cloud_rgbl.cpp` - initMulClassMap()
- `label_color_map.hpp`

**原因**: 预防性添加，以防检测模型输出Label 107

**颜色**: 绿色 (0, 255, 0)

## 使用方法

### 1. 编译代码
```bash
cd /home/youfeng/CLionProjects/05-offline_debug_fusion/offline_perception_debug
mkdir -p build && cd build
cmake ..
make
```

### 2. 运行程序
运行你的离线调试程序，处理包含小球的图像。

### 3. 查看诊断输出
程序会输出详细的诊断信息，重点关注：

**A. 标签分布检查**:
```
[A] lab_out (模型输出) 标签分布:
    Label 104: XXXX pixels
```
→ 如果这里没有Label 104，说明检测模型没有识别到小球

**B. 深度有效性检查**:
```
[3] Depth in ball region:
    - Valid depth pixels: XX / XXXX
```
→ 如果有效深度像素很少，说明视差匹配失败

**C. 采样统计**:
```
Label 104:
  - 有效深度像素: XX (XX%)
```
→ 如果有效深度比例<20%，说明深度质量差

**D. 过滤损失**:
```
Label 104 过滤过程:
  离群点移除后: 0 点
```
→ 如果这里是0，说明被离群点过滤器误删

## 预期效果

修复后，你应该看到：

1. **诊断信息完整**: 能看到Label 104在各阶段的数据
2. **过滤保护生效**: Label 104使用宽松过滤或跳过过滤
3. **点云可视化**: 小球在三视图中以红色显示（Label 104的颜色）

## 如果问题仍然存在

### 场景1: Label 104像素数为0
**原因**: 检测模型没有识别到小球，或filterLabelDect函数没有正确映射
**解决**: 检查检测框是否存在，检查target_id是否为4

### 场景2: 有效深度像素数为0
**原因**: 视差匹配失败，小球区域纹理不足或光照问题
**解决**:
- 调整立体匹配参数
- 检查小球区域的图像质量
- 考虑使用深度补全算法

### 场景3: 采样点数为0
**原因**: 深度值超出范围(0-6m)或采样步长跳过了所有点
**解决**:
- 检查深度值范围
- 减小采样步长（从2改为1）

### 场景4: 过滤后点数为0
**原因**: 即使使用宽松参数，点云仍然太稀疏
**解决**:
- 完全禁用Label 104的离群点过滤
- 减小体素大小（从2.5cm改为1cm）

## 进一步优化建议

1. **深度补全**: 对小球区域进行深度插值
2. **更密集采样**: 对Label 104使用步长1的采样
3. **禁用体素降采样**: 对小目标跳过体素降采样
4. **调整视差参数**: 针对小目标优化立体匹配参数

## 文件修改清单

- ✅ `src/stereo_multi_match.cpp` - 添加诊断日志和特殊过滤策略
- ✅ `src/stereo_point_cloud_rgbl.cpp` - 添加Label 107颜色映射
- ✅ `include/label_color_map.hpp` - 添加Label 107颜色定义

## 回滚方法

如果修改导致问题，可以使用git恢复：
```bash
git checkout src/stereo_multi_match.cpp
git checkout src/stereo_point_cloud_rgbl.cpp
git checkout include/label_color_map.hpp
```
