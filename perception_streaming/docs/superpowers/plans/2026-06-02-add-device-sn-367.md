# 新增设备 SN LK-MR641US000367 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有前端设备列表中新增 `LK-MR641US000367`，将其 SSH 连接端口映射为 `10367`，并把 SSH 私钥配置切换到 `bestmow_rsa_202606`。

**Architecture:** 采用最小配置变更，不重构设备配置结构。沿用 `DevicePanel.vue` 的静态 SN 下拉选项、`useObstacleMonitor.ts` 的 `SN_PORT_MAP` 映射，以及 `data/conf/ssh_config.json` 的 SSH 配置入口。

**Tech Stack:** Vue 3、TypeScript、Vite、JSON 配置文件。

---

## File Structure

- Modify: `src/components/DevicePanel.vue`
  - 责任：展示设备与 Agora 配置表单。新增一个 `<option>`，让用户可以选择 `LK-MR641US000367`。
- Modify: `src/composables/useObstacleMonitor.ts`
  - 责任：维护设备 SN 到 SSH 端口的映射。新增 `LK-MR641US000367: 10367`。
- Modify: `data/conf/ssh_config.json`
  - 责任：提供 SSH 私钥、主机、用户名与默认端口配置。将 `ssh_key_path` 改为 `bestmow_rsa_202606`。
- No new test file
  - 当前项目没有现成测试脚本，验证通过静态搜索与 `npm run build` 完成。

---

### Task 1: 更新设备 SN 下拉列表

**Files:**
- Modify: `src/components/DevicePanel.vue:18-27`

- [ ] **Step 1: 确认当前 SN 下拉选项不包含新设备**

Run:

```bash
rg -n "LK-MR641US000367" src/components/DevicePanel.vue
```

Expected: 命令没有匹配输出，退出码为 1；说明新 SN 尚未加入该文件。

- [ ] **Step 2: 新增下拉选项**

在 `src/components/DevicePanel.vue` 的 SN `<select>` 中，将以下代码：

```vue
              <option value="LK-MR541EU000027">LK-MR541EU000027</option>
              <option value="LK-MR6P1US000286">LK-MR6P1US000286</option>
```

替换为：

```vue
              <option value="LK-MR541EU000027">LK-MR541EU000027</option>
              <option value="LK-MR6P1US000286">LK-MR6P1US000286</option>
              <option value="LK-MR641US000367">LK-MR641US000367</option>
```

- [ ] **Step 3: 验证下拉列表已包含新设备**

Run:

```bash
rg -n "LK-MR641US000367" src/components/DevicePanel.vue
```

Expected: 输出包含 `src/components/DevicePanel.vue` 中新增的 `<option value="LK-MR641US000367">LK-MR641US000367</option>`。

---

### Task 2: 更新 SN 到 SSH 端口映射

**Files:**
- Modify: `src/composables/useObstacleMonitor.ts:7-17`

- [ ] **Step 1: 确认当前端口映射不包含新设备**

Run:

```bash
rg -n "LK-MR641US000367|10367" src/composables/useObstacleMonitor.ts
```

Expected: 命令没有匹配输出，退出码为 1；说明新 SN 和端口尚未加入映射。

- [ ] **Step 2: 新增端口映射**

在 `src/composables/useObstacleMonitor.ts` 的 `SN_PORT_MAP` 中，将以下代码：

```ts
  'LK-MR541EU000027': 10027,
  'LK-MR6P1US000286': 10286,
}
```

替换为：

```ts
  'LK-MR541EU000027': 10027,
  'LK-MR6P1US000286': 10286,
  'LK-MR641US000367': 10367,
}
```

- [ ] **Step 3: 验证映射已包含新设备端口**

Run:

```bash
rg -n "'LK-MR641US000367': 10367" src/composables/useObstacleMonitor.ts
```

Expected: 输出包含 `src/composables/useObstacleMonitor.ts` 中新增的 `'LK-MR641US000367': 10367,`。

---

### Task 3: 切换 SSH 私钥配置

**Files:**
- Modify: `data/conf/ssh_config.json:2`

- [ ] **Step 1: 确认当前私钥配置仍为旧值**

Run:

```bash
rg -n '"ssh_key_path": "bestmow_rsa_202605"' data/conf/ssh_config.json
```

Expected: 输出包含 `data/conf/ssh_config.json:2`，说明当前仍使用旧私钥。

- [ ] **Step 2: 更新私钥文件名**

将 `data/conf/ssh_config.json` 中的：

```json
  "ssh_key_path": "bestmow_rsa_202605",
```

替换为：

```json
  "ssh_key_path": "bestmow_rsa_202606",
```

- [ ] **Step 3: 验证私钥配置已切换**

Run:

```bash
rg -n '"ssh_key_path": "bestmow_rsa_202606"' data/conf/ssh_config.json
```

Expected: 输出包含 `data/conf/ssh_config.json:2`，说明配置已切换到新私钥。

---

### Task 4: 项目级验证

**Files:**
- Validate: `src/components/DevicePanel.vue`
- Validate: `src/composables/useObstacleMonitor.ts`
- Validate: `data/conf/ssh_config.json`

- [ ] **Step 1: 汇总检查三个目标值**

Run:

```bash
rg -n "LK-MR641US000367|10367|bestmow_rsa_202606" src/components/DevicePanel.vue src/composables/useObstacleMonitor.ts data/conf/ssh_config.json
```

Expected: 输出至少包含以下三处：

```text
src/components/DevicePanel.vue:<line>:              <option value="LK-MR641US000367">LK-MR641US000367</option>
src/composables/useObstacleMonitor.ts:<line>:  'LK-MR641US000367': 10367,
data/conf/ssh_config.json:2:  "ssh_key_path": "bestmow_rsa_202606",
```

- [ ] **Step 2: 构建项目验证 TypeScript/Vue 编译无误**

Run:

```bash
npm run build
```

Expected: 构建成功，输出包含 Vite build 完成信息；如果失败，先记录失败输出，再按错误定位修复。

- [ ] **Step 3: 不提交，除非用户明确要求**

本会话遵循“用户明确要求时才提交 commit”。修改完成后只汇报变更和验证结果，不执行 `git commit`。

---

## Self-Review

- Spec coverage: 已覆盖新增设备 SN、端口映射 `10367`、SSH 私钥 `bestmow_rsa_202606`、不重构四项要求。
- Placeholder scan: 未包含 TBD、TODO、implement later、similar to Task N 或未定义步骤。
- Type consistency: SN 字符串、端口数字、JSON 字段名均与现有代码一致。
